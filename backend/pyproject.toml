[project]
name = "backend"
version = "0.1.0"
description = "Backend for Azeroth Tour Guide"
authors = [
    {name = "desmondfowler",email = "desmondfowler14@gmail.com"}
]
requires-python = ">=3.12"
dependencies = [
    "flask (>=3.1.0,<4.0.0)"
]

[tool.poetry]
package-mode = false


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
